/**
 * Sales / site guide bot — problem-first engine (re-export).
 * Implementation: `problemGuideEngine.ts`
 */
export {
  categoryListText,
  getInitialTurn,
  processSalesBotTurn,
  type SalesBotContext,
  type SalesBotMessage,
  type SalesBotMessageTurn,
  type SalesBotTurnResult,
} from './problemGuideEngine'
