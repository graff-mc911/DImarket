import { supabase } from '../supabase'

/** Supabase-клієнт для AI-таблиць (до оновлення generated types). */
// eslint-disable-next-line @typescript-eslint/no-explicit-any
export const aiDb = supabase as any
